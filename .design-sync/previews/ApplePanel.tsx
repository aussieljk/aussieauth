import { ApplePanel } from "@aussieljk/auth";

// A one-click social panel: provider hint above a full-width branded button.

export const Default = () => (
  <div style={{ maxWidth: 380 }}>
    <ApplePanel />
  </div>
);
